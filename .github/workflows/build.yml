name: Build APK

# Telegram-FOSS specific CI (fork base pins: Gradle 7.2 / AGP 7.0.3 / JDK 11 /
# compileSdk 33 / buildTools 33.0.0 / NDK 21.4.7075529 / cmake 3.10.2).
# Do NOT reuse generic Telegram-Android (DrKLO) workflows here.

on:
  push:
    branches: [develop]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:
    inputs:
      abi:
        description: 'ABI set to build'
        required: false
        default: 'arm64'
        type: choice
        options: [arm64, all]

# github.token needs contents:write to create Releases and upload the APK.
permissions:
  contents: write

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: "Checkout (recursive submodules: ffmpeg, boringssl, libvpx, libwebp)"
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Set up JDK 11 (Gradle 7.2 / AGP 7.0.3 requirement)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'
          cache: gradle

      - name: Free runner disk (preinstalled NDKs are useless for our pin)
        run: |
          echo "Before:"; df -h / | tail -1
          sudo rm -rf "$ANDROID_HOME/ndk" "$ANDROID_HOME/ndk-bundle"
          echo "After:"; df -h / | tail -1

      - name: Install pinned NDK 21.4.7075529
        run: |
          # sdkmanager needs Java 17+, but build needs JDK 11 - use runner JVM for sdkmanager only
          export JAVA_HOME="${JAVA_HOME_21_X64:-${JAVA_HOME_17_X64:-$JAVA_HOME}}"
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses > /dev/null 2>&1 || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "ndk;21.4.7075529" > /dev/null

      - name: Write API_KEYS (dummy values; MTProto transport is being replaced by REST)
        run: |
          printf 'APP_ID=1\nAPP_HASH=0123456789abcdef0123456789abcdef\n' > API_KEYS

      - name: ABI filter to arm64-v8a (CI-only transform, never committed)
        if: ${{ github.event_name == 'push' || inputs.abi == 'arm64' }}
        run: |
          sed -i 's/abiFilters "armeabi-v7a", "arm64-v8a", "x86", "x86_64"/abiFilters "arm64-v8a"/' TMessagesProj_App/build.gradle
          # Library module has no abiFilters of its own -> AGP builds ALL ABIs there.
          # Inject arm64-only filter after the unique APP_HASH anchor in defaultConfig.
          sed -i "s/buildConfigField 'String', 'APP_HASH', getAppHash()/buildConfigField 'String', 'APP_HASH', getAppHash()\n\n        ndk { abiFilters \"arm64-v8a\" }/" TMessagesProj/build.gradle
          echo "app module:"; grep -c 'abiFilters "arm64-v8a"' TMessagesProj_App/build.gradle
          echo "lib module:"; grep -c 'abiFilters "arm64-v8a"' TMessagesProj/build.gradle

      - name: "Build native prebuilts (libvpx, ffmpeg, boringssl for arm64)"
        run: |
          # Per upstream README: NDK + Ninja required; order matters
          sudo apt-get install -y ninja-build > /dev/null
          export NDK="$ANDROID_HOME/ndk/21.4.7075529"
          export NINJA_PATH="$(which ninja)"
          cd TMessagesProj/jni
          ./build_libvpx_clang.sh arm64
          ./build_ffmpeg_clang.sh arm64
          ./patch_ffmpeg.sh
          ./patch_boringssl.sh
          ./build_boringssl.sh arm64

      - name: Build assembleAfatDebug
        run: |
          ./gradlew :TMessagesProj_App:assembleAfatDebug \
            -Dorg.gradle.jvmargs="-Xmx4g -XX:MaxMetaspaceSize=1g" \
            --stacktrace

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-${{ github.run_number }}
          path: TMessagesProj_App/build/outputs/apk/afat/debug/*.apk
          retention-days: 14
          if-no-files-found: error

      - name: Stage APK for release (rename + sha256)
        if: success()
        run: |
          mkdir -p release
          APK="$(ls TMessagesProj_App/build/outputs/apk/afat/debug/*.apk | head -n 1)"
          cp "$APK" "release/Telegram-FOSS-debug-arm64-build-${GITHUB_RUN_NUMBER}.apk"
          sha256sum release/Telegram-FOSS-*.apk > release/SHA256SUMS.txt

      - name: Publish APK to GitHub Releases (installable build for testing)
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="build-${GITHUB_RUN_NUMBER}"
          NOTES="Automated debug build of develop @ ${GITHUB_SHA::7} (afat/debug, arm64-v8a). Install: download APK, allow unknown sources, install. sha256 in SHA256SUMS.txt."
          if gh release view "$TAG" -R "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$TAG" release/* --clobber -R "$GITHUB_REPOSITORY"
          else
            gh release create "$TAG" release/* \
              --target "$GITHUB_SHA" \
              --title "Debug build ${GITHUB_RUN_NUMBER} (arm64-v8a, installable)" \
              --notes "$NOTES" \
              --prerelease \
              -R "$GITHUB_REPOSITORY"
          fi
